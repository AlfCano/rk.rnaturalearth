# rk.rnaturalearth
It is a user-friendly wrapper for the `rnaturalearth`, `sf`, and `ggspatial` packages within RKWard. It allows users to generate high-quality administrative Choropleth maps (heat maps based on regions) without needing complex GIS knowledge or shapefile management.
